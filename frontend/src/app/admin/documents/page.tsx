import DocumentsTable from "@/components/admin/DocumentsTable";

export const metadata = { title: "Admin — Documents | Veritas" };

export default function DocumentsPage() {
  return <DocumentsTable />;
}
